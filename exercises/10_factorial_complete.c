#include <stdio.h>

// Complete this function to calculate the factorial of a number
int factorial(int n) {
    // Write the code here
}

int main() {
    int num = 5;
    int result = factorial(num);
    printf("%d 的阶乘是: %d\n", num, result);
    return 0;
}