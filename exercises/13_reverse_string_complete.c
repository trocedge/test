#include <stdio.h>
#include <string.h>

// Complete this function, inverting the string
void reverseString(char str[]) {
    // Write the code here
}

int main() {
    char str[] = "Hello";
    reverseString(str);
    printf("Reversed string is: %s\n", str);
    return 0;
}