#include <stdio.h>

// Complete this function to determine whether it is a leap year
int isLeapYear(int year) {
    // Write the code here
}

int main() {
    int year = 2024;
    if (isLeapYear(year)) {
        printf("%d is a leap year\n", year);
    } else {
        printf("%d is not a leap year\n", year);
    }
    return 0;
}